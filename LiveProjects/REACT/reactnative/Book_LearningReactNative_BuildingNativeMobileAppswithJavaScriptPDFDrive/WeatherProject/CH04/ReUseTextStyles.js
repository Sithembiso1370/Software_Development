import { StyleSheet } from 'react-native';

var styles = StyleSheet.create({
    bold: {
    fontWeight: "bold",
    color: "#FF0000"
    },
    italic: {
    fontStyle: "italic"
    }
   });


   export default styles;